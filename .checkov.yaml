skip-checks:
  - CKV_K8S_43  # Image digest pinning: image tag is set dynamically by CI/CD; SHA digest pinning not compatible with this pipeline
  - CKV_K8S_35  # App reads runtime secrets from env vars; deployment injects them from Kubernetes/GitHub secrets
